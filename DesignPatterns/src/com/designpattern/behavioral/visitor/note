使用访问者模式可以实现双分派（double dispatch）.原因如下：
1. 对象的操作权限被分配到了访问者，访问者执行对象的操作是通过其委托（即引用）执行的，属于对象主动执行自己
2. 访问者对象执行自己的方法也是主动执行。

注意：
java通过访问者模式实现双分派其实也必须遵循一个原则：迪米特法则(尽量自己完成自己的工作，而不是让访问者类通过构建多个方法处理不同实现的对象)
实现双分派的要诀是： 将本该属于对象的方法尽量转回对象中，封装差异。